<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GIML.NewInstance"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GIML"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Style="{StaticResource SubtleButtonStyle}"
                Click="CancelButton_Click"
                Grid.Column="0"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Back" Height="12" Width="12" VerticalAlignment="Center"/>
                    <TextBlock Text="返回" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <TextBlock Text="创建新实例" Grid.Column="1" VerticalAlignment="Center" Foreground="Gray"/>
            
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="28"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Canvas Grid.Row="0">
                    <StackPanel Orientation="Horizontal" Spacing="8" Canvas.Left="20">
                        <TextBlock Text="筛选：" VerticalAlignment="Center"/>
                        <ComboBox SelectionChanged="FliterBox_SelectionChanged" SelectedIndex="0" Width="350">
                            <x:String>原版（Mindustry）</x:String>
                            <x:String>BE（Mindustry-BuildEdge）</x:String>
                            <x:String>学术端（Mindustry-CN-ARC）</x:String>
                            <x:String>X端（MindustryX）</x:String>
                        </ComboBox>
                    </StackPanel>
                </Canvas>
                <GridView Grid.Row="1">

                </GridView>
            </Grid>
            
            <Border Grid.Column="1" 
                BorderThickness="1,0,0,0"
                BorderBrush="Gray"
                Padding="0"/>
            
        </Grid>
    </Grid>
</Page>
