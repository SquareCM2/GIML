<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GIML.RunningInstance"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GIML"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="15, 15, 15, 15" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Text=" 实例运行中……"/>

        <ScrollViewer x:Name="OutputScrollViewer" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled" Grid.Row="1" Background="Black" CornerRadius="10">
            <TextBlock x:Name="OutputTextBlock" 
                       TextWrapping="Wrap"
                       FontFamily="Consolas"
                       IsTextSelectionEnabled="True"
                       Margin="10"/>
        </ScrollViewer>

        <!-- 底部按钮：返回/终止 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button x:Name="CloseButton" 
                    Content="关闭并返回" 
                    Click="CloseButton_Click" 
                    Margin="5"
                    Padding="12,6"/>
        </StackPanel>
    </Grid>
</Page>
