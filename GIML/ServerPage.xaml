<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GIML.ServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GIML"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>
    
    <Grid RowSpacing="10" Padding="20, 10, 20, 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="服务器" Style="{StaticResource TitleTextBlockStyle}"/>

        <Grid Background="#80000000" 
            Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" Grid.Row="1">
            <ProgressRing x:Name="LoadingRing" 
                  IsActive="{x:Bind IsLoading, Mode=OneWay}" 
                  Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" 
                  HorizontalAlignment="Center" 
                  VerticalAlignment="Center"
                  Width="50" Height="50"/>
        </Grid>
        
        <GridView Grid.Row="2"
          x:Name="ServersGridView"
          SelectionMode="None"
          Margin="20, 0" ItemsSource="{x:Bind Servers, Mode=OneWay}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:ServerInfo">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefault}"
                    Width="700" Height="110" CornerRadius="5" Padding="5">
                        <Grid Margin="12" ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <RichTextBlock local:RichTextHelper.FormattedText="{x:Bind Name, Mode=OneWay}" FontSize="16" FontFamily="Segoe UI, ms-appx:///Assets/Fonts/font.woff#fontello"/>
                                <StackPanel Orientation="Horizontal" Spacing="20">
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <TextBlock Text="版本" Foreground="Gray"/>
                                        <TextBlock Text="{x:Bind Version}" Foreground="Gray"/>
                                    </StackPanel>
                                    <TextBlock Text="{x:Bind Address}" Foreground="Gray"/>
                                </StackPanel>
                                <RichTextBlock local:RichTextHelper.FormattedText="{x:Bind Desc, Mode=OneWay}" FontSize="12" FontFamily="Segoe UI, ms-appx:///Assets/Fonts/font.woff#fontello"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                    <TextBlock Text="{x:Bind Players}" Foreground="Gray"/>
                                    <TextBlock Text="/" Foreground="Gray"/>
                                    <TextBlock Text="{x:Bind Limit_Str}" Foreground="Gray"/>
                                </StackPanel>
                                <RichTextBlock local:RichTextHelper.FormattedText="{x:Bind MapName, Mode=OneWay}" FontSize="14" FontFamily="Segoe UI, ms-appx:///Assets/Fonts/font.woff#fontello"/>
                                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                    <TextBlock Text="模式：" Foreground="Gray" FontSize="12"/>
                                    <TextBlock Text="{x:Bind Mode}" Foreground="Gray" FontSize="12"/>
                                    <TextBlock Text="，波次：" Foreground="Gray" FontSize="12"/>
                                    <TextBlock Text="{x:Bind Wave}" Foreground="Gray" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Spacing="20">
                                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                    <TextBlock Text="服务端：" Foreground="Gray" FontSize="12"/>
                                    <TextBlock Text="{x:Bind Type}" Foreground="Gray" FontSize="12"/>
                                </StackPanel>
                                <Button Width="150" Height="40" Click="CopyAddressButton_Click" Tag="{x:Bind}">
                                    <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <FontIcon Glyph="&#xE8C8;" />
                                        <TextBlock Text="复制地址"></TextBlock>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
