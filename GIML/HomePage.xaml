<?xml version="1.0" encoding="utf-8"?>
    
<Page
    x:Class="GIML.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GIML"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <local:NullToBoolConverter x:Key="NullToBoolConverter"/>
        <!-- 实例卡片模板 -->
        <DataTemplate x:Key="InstanceTemplate" x:DataType="local:GameInstance">
            <Grid Width="155" Height="155" Margin="10" Background="{ThemeResource CardBackgroundFillColorDefault}" CornerRadius="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Image Source="{x:Bind IconPath}" Width="64" Height="110" Grid.Row="0"/>
                <TextBlock Text="{x:Bind Name}" HorizontalAlignment="Center" Grid.Row="1"/>
                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                    <TextBlock Text="{x:Bind Type}" Foreground="Gray" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock Text="{x:Bind Version}" Foreground="Gray" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- 添加占位符模板 -->
        <DataTemplate x:Key="AddTemplate" x:DataType="local:AddInstancePlaceholder">
            <Grid Width="155" Height="155" Margin="10" Background="{ThemeResource CardBackgroundFillColorSecondary}" CornerRadius="8">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE710;" Width="100" Height="100" FontSize="32"/>
                    <TextBlock Text="添加新实例" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- 模板选择器 -->
        <local:GameItemTemplateSelector x:Key="TemplateSelector"
                                    InstanceTemplate="{StaticResource InstanceTemplate}"
                                    AddTemplate="{StaticResource AddTemplate}"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="64"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" HorizontalAlignment="Center" Width="720">
            <ScrollView Padding="0, 10, 0 0">
                <GridView ItemsSource="{x:Bind GameItems, Mode=OneWay}"
                    SelectionMode="Single"
                    SelectedItem="{x:Bind SelectedObject, Mode=TwoWay}"
                    SelectionChanged="GridView_SelectionChanged" HorizontalAlignment="left">
                    <GridView.ItemTemplateSelector>
                        <local:GameItemTemplateSelector InstanceTemplate="{StaticResource InstanceTemplate}"
                              AddTemplate="{StaticResource AddTemplate}"/>
                    </GridView.ItemTemplateSelector>
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid MaximumRowsOrColumns="4" Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                </GridView>
            </ScrollView>
        </Grid>

        <Border Grid.Row="1" 
            BorderThickness="0,1,0,0"
            BorderBrush="Gray"
            Padding="0"/>

        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal" Spacing="8" Padding="8, 0, 8, 0">
                <Button x:Name="LaunchClient" 
                    Content="启动游戏" 
                    Width="100" Height="36"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    IsEnabled="{x:Bind SelectedInstance, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}"
                    Click="LaunchButton_Click"
                    Margin="5"/>
                <Button x:Name="EditInstance" 
                    Content="编辑实例" 
                    Width="100" Height="36"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    IsEnabled="{x:Bind SelectedInstance, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}"
                    Click="EditInstanceButton_Click"
                    Margin="5"/>
            </StackPanel>
            
        </Grid>
    </Grid>
</Page>
